export default class DataFile{

    constructor(name, columnNames, numColumns, numRows, stats ){
        this.name = name;
        this.columnNames = columnNames;
        this.numColumns = numColumns;
        this.numRows = numRows;
    }



}